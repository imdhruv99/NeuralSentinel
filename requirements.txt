# confluent-kafka wraps librdkafka (the C client real systems run on). It speaks
# Kafka 3.9's flexible (KIP-482) protocol correctly, unlike pure-Python
# kafka-python 3.0.0, whose consumer fetch path aborts with InvalidReceiveError
# against this broker. Single client for producer, consumer, and topic admin.
confluent-kafka==2.14.2
pydantic==2.13.4
pydantic-settings==2.14.1
PyYAML==6.0.3
psycopg==3.3.4
psycopg-binary==3.3.4
redis==8.0.0
numpy==2.4.6
pandas==2.3.3
mlflow==3.14.0
scikit-learn==1.9.0
torch==2.12.1
fastapi==0.115.14
uvicorn[standard]==0.35.0
asyncpg==0.30.0
